apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert --volumes hostPath
    kompose.version: 1.22.0 (HEAD)
  creationTimestamp: null
  labels:
    io.kompose.service: alertmanager
  name: alertmanager
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: alertmanager
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert --volumes hostPath
        kompose.version: 1.22.0 (HEAD)
      creationTimestamp: null
      labels:
        io.kompose.service: alertmanager
    spec:
      containers:
        - args:
            - --config.file=/alertmanager.yml
          image: prom/alertmanager
          name: alertmanager
          ports:
            - containerPort: 9093
          resources: {}
          securityContext:
            privileged: true
          volumeMounts:
            - mountPath: /alertmanager.yml
              name: alertmanager-hostpath0
      restartPolicy: Always
      volumes:
        - hostPath:
            path: /Users/stephanesbizzera/PycharmProjects/agnostic/setup/alertmanager/alertmanager.yml
          name: alertmanager-hostpath0
status: {}
